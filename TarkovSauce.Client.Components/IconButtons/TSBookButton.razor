@using TarkovSauce.Client.Components.Buttons
@inherits ButtonBase

<div class="@CssImpl" style="@StyleImpl">
    <button @onclick="async _ => await OnClick.InvokeAsync()" title="@Label">
        <i class="bi-book" />
    </button>
</div>

<style>
    .icon-button button i.bi-book:before {
        animation: popIn 0.5s forwards;
    }

    .icon-button button:hover i.bi-book:before {
        content: '\F192';
        animation: popOut 0.5s forwards;
    }

</style>

@code {
    protected override string CssImpl => $"tsbookbutton icon-button {base.CssImpl}";
}